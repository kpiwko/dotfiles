#!/usr/bin/env bash
set -euo pipefail

for arg in "$@"; do
    case "$arg" in
        -exec|-execdir|-delete|-ok|-okdir)
            echo "sandbox-find: prohibited option: $arg" >&2
            exit 2
            ;;
    esac
done

exec find "$@"
