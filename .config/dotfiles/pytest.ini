[pytest]
testpaths = tests
addopts = -ra
markers =
    binary(name): test is owned by a .local/bin executable
    caddy: caddy configuration/service integration test
    k8s: kubernetes manifest test
